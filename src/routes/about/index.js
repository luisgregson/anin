import { h } from 'preact';
import style from './style';

const About = () => (
  <div class={style.about}>
    <h2>About</h2>
    <p>This page contains details about the band.</p>
  </div>
);

export default About;
